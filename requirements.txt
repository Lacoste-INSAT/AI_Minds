# MemoryGraph — Python Dependencies
# Pin versions after initial install: pip freeze > requirements.lock

# === Core Framework ===
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0

# === LLM Client ===
httpx>=0.28               # async Ollama API calls

# === Vector Store ===
qdrant-client>=1.13

# === Embeddings ===
sentence-transformers>=3.4
numpy>=1.26

# === Sparse Search ===
rank-bm25>=0.2.2

# === Knowledge Graph ===
networkx>=3.4             # in-memory graph analysis
# SQLite is stdlib — no extra dep

# === NLP / NER ===
spacy>=3.8
# Post-install: python -m spacy download en_core_web_sm

# === Document Parsing ===
pymupdf>=1.25             # PDF extraction (import fitz)
python-docx>=1.1          # DOCX extraction
Pillow>=11.0              # image handling
pytesseract>=0.3          # OCR

# === Audio Transcription ===
faster-whisper>=1.1       # tiny model, CPU-friendly

# === File Watching ===
watchdog>=6.0

# === Scheduling ===
apscheduler>=3.10

# === Logging ===
structlog>=24.4

# === WebSocket (included with uvicorn[standard]) ===
websockets>=14.0
